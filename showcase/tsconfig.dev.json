{
	"extends": "../tsconfig",
	"include": ["*.config.ts", "package.json"],
	"compilerOptions": {
		"composite": true,
		"rootDir": "${configDir}",
		"outDir": "${configDir}/lib",
		"resolveJsonModule": true
	}
}
