{
	"extends": "../tsconfig",
	"compilerOptions": {
		"types": ["node"],
		"strict": true
	},
	"include": ["${configDir}/src/**/*.ts", "${configDir}/src/**/*.tsx", "${configDir}/src/**/*.json"],
	"exclude": ["${configDir}/src/internal/target"]
}
